apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storage-service.fullname" . }}-test-connection"
  labels:
    {{- include "storage-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storage-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
